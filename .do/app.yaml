# DigitalOcean App Platform spec for Jev Ticket Triage.
#   doctl apps create --spec .do/app.yaml
# or paste into the App Platform UI (app Settings -> App Spec -> Edit).
name: jev-ticket-triage
region: nyc
services:
  - name: web
    github:
      repo: paddix/JEV
      branch: main
      deploy_on_push: true
    environment_slug: python
    run_command: gunicorn --worker-tmp-dir /dev/shm --bind 0.0.0.0:8080 app:app
    http_port: 8080
    instance_count: 1
    instance_size_slug: basic-xxs
    health_check:
      http_path: /healthz
    # NOTE: TYPESAFE_API_KEY is intentionally NOT in this spec.
    # It's set as an encrypted App-Level env var in the App Platform UI.
    # Defining it here with a blank value would override the real key.
    envs:
      - key: JEV_MODEL
        scope: RUN_TIME
        value: jev-latest
